// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_URL")
}

model Gasto {
  id        Int     @id @default(autoincrement())
  user   User @relation(fields: [usuarioId], references: [id])
  usuarioId Int     @unique
  monto     Int     // Nuevo campo para almacenar el monto del gasto
  tipoDeGasto String // Nuevo campo para almacenar el tipo de gasto
}

model User {
  id              Int      @id @default(autoincrement())
  name            String
  money  Int
  gastos          Gasto[]  // Relación uno a muchos con la tabla Gasto
}





